# MongoDB

Esta carpeta contiene el modelo de datos en MongoDB.

Archivos:

- collections.md: define la estructura de las colecciones
- indexes.py: crea los índices para mejorar consultas
- pipelines.py: incluye consultas con agregación

Uso:

1. Ejecutar indexes.py para crear los índices
2. Usar pipelines.py para hacer consultas